{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:microsocks-clean}/test.db",
            "name": "msdb"
        }
    ]
}